/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { IterateObjectByOrderPipe } from './iterate-object-by-order.pipe';

describe('IterateObjectByOrderPipe', () => {
  it('create an instance', () => {
    let pipe = new IterateObjectByOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
